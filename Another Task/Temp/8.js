function calculateParallelogramArea(base, height) {
    return base * height;
}

const baseLength = 5;
const height = 8;

const area = calculateParallelogramArea(baseLength, height);
console.log("Parallelogram Area:", area);
